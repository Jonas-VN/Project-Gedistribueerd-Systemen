All chats will be stored in this folder if you save a chat. 

The chats are stored like this:
- Chats/
  - `<prompted_file_name>`/
    - `<username>`.chat
    - `<username>`.chat
    - ...
  - `<prompted_file_name>`/
    - `<username>`.chat
    - `<username>`.chat
    - ...
  - ...